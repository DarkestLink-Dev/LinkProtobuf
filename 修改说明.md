# LinkProtobuf 插件修改说明

## 修改日期
2025-11-05

## 问题描述
原插件在运行时报错：`Proto Descriptor for PlayerShootMessage not found`

**根本原因**：protoc 生成的 `.pb.cc` 文件未被 UBT（Unreal Build Tool）自动编译，导致 protobuf 的描述符未注册到全局池中。

## 解决方案
修改生成逻辑，让 protoc 生成后自动将 `.pb.cc` 重命名为 `.pb.cpp`，确保 UBT 自动扫描并编译这些文件。

## 修改内容

### 1. 修改的文件
- `Source/LinkProtobufEditor/Private/LinkProtobufEditorFunctionLibrary.cpp`（第 363-403 行）

### 2. 具体修改
在 `GenerateProtoCppFile()` 函数中，protoc 执行成功后自动重命名生成的文件：

```cpp
// 新增：捕获生成路径和文件名
FString CapturedGenPath = AbsGenPath;
FString CapturedProtoFileName = ULinkProtobufEditorSettings::Get()->ProtoFileName;

// 在 lambda 中添加重命名逻辑
if (ReturnCode == 0)
{
    FString PbCcPath = FPaths::Combine(CapturedGenPath, CapturedProtoFileName + TEXT(".pb.cc"));
    FString PbCppPath = FPaths::Combine(CapturedGenPath, CapturedProtoFileName + TEXT(".pb.cpp"));
    
    // 删除旧的 .pb.cpp（如果存在）
    if (IFileManager::Get().FileExists(*PbCppPath))
    {
        IFileManager::Get().Delete(*PbCppPath, false, true);
    }
    
    // 重命名 .cc → .cpp
    if (IFileManager::Get().Move(*PbCppPath, *PbCcPath, true, true))
    {
        UE_LOG(LogProtoEditor, Display, TEXT("Renamed %s to %s for UBT compilation"), *PbCcPath, *PbCppPath);
    }
}
```

### 3. 手动处理现有文件
已将 `User.pb.cc` 重命名为 `User.pb.cpp`

## 使用流程（修改后）

### 首次应用修改
1. **关闭 UE 编辑器**
2. **右键 `.uproject` → Generate Visual Studio project files**
3. **在 IDE（VS/Rider）中完整重新编译**（Clean + Build）
4. **启动编辑器测试**

### 日常使用（新增/修改结构体）
1. 在编辑器中修改 UStruct 或在 `Edit → ProjectSettings → LinkProtobuf` 中添加新结构体
2. 点击 `Generate ProtoFiles From Settings` 按钮
3. **等待生成完成**（插件会自动重命名 .pb.cc → .pb.cpp）
4. **关闭编辑器**
5. **在 IDE 中重新编译项目**（或使用 Live Coding，但完整编译更可靠）
6. **重启编辑器**

## 优势
- ✅ **完全自动化**：每次生成后自动重命名，无需手动操作
- ✅ **跨平台兼容**：Windows、Android、Linux、iOS 等所有平台都支持
- ✅ **易维护**：新增/修改结构体只需点击生成 + 重编译
- ✅ **UBT 原生支持**：.cpp 文件会被 UBT 自动发现和编译

## 跨平台说明
此方案对所有平台 100% 兼容：

| 平台 | 兼容性 | 说明 |
|------|--------|------|
| Windows (x64/ARM64) | ✅ | 已测试验证 |
| Android (所有 ABI) | ✅ | 静态库已配置，UBT 统一处理 |
| Linux (x64/ARM64) | ✅ | 与 Android 同理 |
| iOS/Mac | ✅ | 机制相同，预期兼容 |

**原理**：
1. UBT 对所有平台统一处理 `.cpp` 文件
2. 重命名不改变代码内容，只是让 UBT 能发现并编译
3. Build.cs 已为所有平台配置好 protobuf 静态库

## 注意事项
1. **每次生成后必须完整重编译**（不能只用 Hot Reload）
2. **如果修改了 proto 定义，旧的二进制数据可能不兼容**
3. **生成的文件位于**：`Plugins/UE-LinkProtobuf/Source/LinkProtobufRuntime/Public/ProtoSource/`

## 故障排查

### 如果仍然报 "Descriptor not found"
1. 确认 `User.pb.cpp` 存在于 `ProtoSource` 目录
2. 确认执行了完整重编译（不是 Live Coding）
3. 检查编译输出，确保 `User.pb.cpp` 被编译了
4. 查看编辑器日志中是否有 "Renamed ... for UBT compilation" 消息

### 如果生成失败
1. 检查 protoc.exe 是否存在于 `Source/ThirdParty/Win64/`
2. 查看编辑器输出日志中的错误信息
3. 确认 ProtoSource 目录有写入权限

## 参考
- 原插件仓库：https://github.com/DarkestLink-Dev/UE-LinkProtobuf
- Protobuf 官方文档：https://protobuf.dev/

