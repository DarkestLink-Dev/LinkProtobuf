# LinkProtobuf 插件使用说明

**版本**: 1.0 (Modified)  
**最后更新**: 2025-11-05  
**适用引擎**: UE 5.2 - 5.6

---

## 📖 插件简介

本插件基于 [UE-LinkProtobuf](https://github.com/DarkestLink-Dev/UE-LinkProtobuf) 修改，增强了生成流程的自动化：
- ✅ 自动将生成的 `.pb.cc` 重命名为 `.pb.cpp`，确保 UBT 自动编译
- ✅ 支持 Windows、Android、Linux 等所有平台
- ✅ 简化了日常使用流程

---

## 🚀 快速开始

### 一、在现有项目中使用

#### 1. 定义消息结构体

在你的项目中创建 C++ 结构体（必须是 C++ 项目）：

```cpp
// 例如：YourProject/Source/YourModule/Public/GameMessages.h
#pragma once

#include "CoreMinimal.h"
#include "GameMessages.generated.h"

// 玩家射击消息
USTRUCT(BlueprintType)
struct FPlayerShootMessage
{
    GENERATED_BODY()

    UPROPERTY(BlueprintReadWrite, Category = "Network")
    int32 PlayerId;

    UPROPERTY(BlueprintReadWrite, Category = "Network")
    FString NickName;

    UPROPERTY(BlueprintReadWrite, Category = "Network")
    FVector ShootDirection;
};

// 玩家移动消息
USTRUCT(BlueprintType)
struct FPlayerMoveMessage
{
    GENERATED_BODY()

    UPROPERTY(BlueprintReadWrite, Category = "Network")
    int32 PlayerId;

    UPROPERTY(BlueprintReadWrite, Category = "Network")
    FVector Position;

    UPROPERTY(BlueprintReadWrite, Category = "Network")
    FRotator Rotation;
};
```

#### 2. 在编辑器中配置结构体

1. 打开 UE 编辑器
2. 进入 `Edit → Project Settings`
3. 找到 `Plugins → Link Protobuf → User Defined Structs for Protobuf`
4. 点击 `+` 按钮添加你的结构体：
   - 搜索并选择 `FPlayerShootMessage`
   - 搜索并选择 `FPlayerMoveMessage`
   - ...（添加所有需要序列化的结构体）

#### 3. 生成 Proto 文件

1. 在同一设置页面，点击 **`Generate ProtoFiles From Settings`** 按钮
2. 等待几秒，会弹出通知：
   - ✅ 成功：`ProtoCpp Generate Success. Please recompile your project from IDE.`
   - ❌ 失败：查看 Output Log 中的错误信息

#### 4. 重新编译项目

**重要：每次生成后必须重新编译！**

1. **关闭 UE 编辑器**
2. 右键点击 `.uproject` 文件 → `Generate Visual Studio project files`
3. 打开 Visual Studio 或 Rider
4. 执行 `Clean Solution`（清理解决方案）
5. 执行 `Build Solution`（生成解决方案）
6. 等待编译完成（首次可能需要几分钟）

#### 5. 启动编辑器并测试

重新启动编辑器后，你就可以在蓝图或 C++ 中使用序列化功能：

**蓝图节点**：
- `Convert Struct To Proto Binary Bytes`：将 UStruct → 二进制数据
- `Convert Proto Binary Bytes To Struct`：将二进制数据 → UStruct

**C++ 代码**：
```cpp
#include "LinkProtobufFunctionLibrary.h"

// 序列化（UStruct → 二进制）
FPlayerShootMessage ShootMsg;
ShootMsg.PlayerId = 1;
ShootMsg.NickName = TEXT("玩家测试");

TArray<uint8> BinaryData;
bool bSuccess = ULinkProtobufFunctionLibrary::ConvertStructToBinaryProtoBytes(
    FPlayerShootMessage::StaticStruct(),
    &ShootMsg,
    BinaryData
);

if (bSuccess)
{
    // 发送 BinaryData 到服务器
    SendToServer(BinaryData);
}

// 反序列化（二进制 → UStruct）
FPlayerShootMessage ReceivedMsg;
bool bParsed = ULinkProtobufFunctionLibrary::ConvertProtoBinaryBytesToStruct(
    FPlayerShootMessage::StaticStruct(),
    false, // bAllowIncomplete
    BinaryData,
    &ReceivedMsg
);
```

---

### 二、在新项目中使用

#### 1. 复制插件到新项目

```
旧项目/Plugins/UE-LinkProtobuf/
    ↓ 复制整个文件夹
新项目/Plugins/UE-LinkProtobuf/
```

#### 2. 清理旧的生成文件（重要！）

进入 `新项目/Plugins/UE-LinkProtobuf/Source/LinkProtobufRuntime/Public/ProtoSource/` 目录，**删除所有文件**：
- ❌ 删除 `User.proto`（旧项目的消息定义）
- ❌ 删除 `User.pb.h`（旧项目生成的头文件）
- ❌ 删除 `User.pb.cpp`（旧项目生成的源文件）
- ❌ 删除其他所有 `.proto`、`.pb.h`、`.pb.cpp` 文件

**为什么要删除？** 这些是旧项目的消息定义，会和新项目的消息冲突。

#### 3. 确保新项目是 C++ 项目

如果新项目是纯蓝图项目，需要先添加 C++ 支持：
1. 在编辑器中：`File → New C++ Class`
2. 选择 `None`（空类）或任意类型
3. 创建后 UE 会自动生成 C++ 项目结构

#### 4. 启用插件

1. 打开新项目的编辑器
2. `Edit → Plugins`
3. 搜索 `LinkProtobuf`
4. 勾选 `Enabled`
5. 重启编辑器

#### 5. 按照"在现有项目中使用"的步骤操作

从"定义消息结构体"开始，重复上面的 1-5 步。

---

## 📝 日常使用流程

### 场景 1：新增一个消息结构体

```
1. 在 C++ 中定义新的 USTRUCT
   ↓
2. 编译项目（让 UE 识别新结构体）
   ↓
3. 在编辑器设置中添加该结构体
   ↓
4. 点击 "Generate ProtoFiles From Settings"
   ↓
5. 关闭编辑器 → IDE 重新编译 → 重启编辑器
   ↓
6. 新结构体可以使用了！
```

### 场景 2：修改现有消息结构体

```
1. 在 C++ 中修改 USTRUCT（如增加字段）
   ↓
2. 编译项目
   ↓
3. 在编辑器中点击 "Generate ProtoFiles From Settings"
   ↓
4. 关闭编辑器 → IDE 重新编译 → 重启编辑器
   ↓
5. 修改生效！
```

**注意**：修改消息定义后，旧的二进制数据可能无法解析（protobuf 向后兼容有规则）。

### 场景 3：删除不再使用的消息结构体

```
1. 在编辑器设置中移除该结构体
   ↓
2. 点击 "Generate ProtoFiles From Settings"
   ↓
3. 关闭编辑器 → IDE 重新编译 → 重启编辑器
```

---

## 🎯 常见使用场景示例

### 示例 1：客户端发送射击消息到服务器

```cpp
void AMyCharacter::SendShootMessage()
{
    // 1. 构造消息
    FPlayerShootMessage Msg;
    Msg.PlayerId = GetPlayerID();
    Msg.NickName = GetPlayerName();
    Msg.ShootDirection = GetControlRotation().Vector();

    // 2. 序列化为二进制
    TArray<uint8> BinaryData;
    bool bSuccess = ULinkProtobufFunctionLibrary::ConvertStructToBinaryProtoBytes(
        FPlayerShootMessage::StaticStruct(),
        &Msg,
        BinaryData
    );

    if (bSuccess)
    {
        // 3. 通过网络发送（例如 KCP/TCP/UDP）
        MyNetworkManager->SendToServer(501, BinaryData);
        UE_LOG(LogTemp, Log, TEXT("射击消息已发送，大小: %d 字节"), BinaryData.Num());
    }
    else
    {
        UE_LOG(LogTemp, Error, TEXT("射击消息序列化失败"));
    }
}
```

### 示例 2：服务器接收并解析消息

```cpp
void AMyGameMode::OnReceiveMessage(int32 RouteID, const TArray<uint8>& BinaryData)
{
    if (RouteID == 501) // 射击消息
    {
        FPlayerShootMessage Msg;
        bool bParsed = ULinkProtobufFunctionLibrary::ConvertProtoBinaryBytesToStruct(
            FPlayerShootMessage::StaticStruct(),
            false,
            BinaryData,
            &Msg
        );

        if (bParsed)
        {
            UE_LOG(LogTemp, Log, TEXT("收到射击消息: 玩家ID=%d, 昵称=%s"), 
                Msg.PlayerId, *Msg.NickName);
            
            // 处理射击逻辑
            ProcessShoot(Msg);
        }
    }
}
```

### 示例 3：蓝图中使用

1. 创建结构体变量（例如 `FPlayerShootMessage` 类型）
2. 填充字段值
3. 拖出变量，搜索 `Convert Struct To Proto Binary Bytes`
4. 连接到网络发送节点

---

## ⚙️ 高级配置

### 修改生成文件名

默认生成的文件名是 `User.proto`，可以修改：

1. `Edit → Project Settings → Link Protobuf → Proto File Name`
2. 改为你想要的名字（如 `GameMessages`）
3. 生成时会产生 `GameMessages.proto`、`GameMessages.pb.cpp` 等

### 查看生成的文件

生成的文件位于：
```
项目/Plugins/UE-LinkProtobuf/Source/LinkProtobufRuntime/Public/ProtoSource/
├── User.proto          (proto 定义)
├── User.pb.h           (C++ 头文件)
└── User.pb.cpp         (C++ 源文件，会被自动编译)
```

---

## ⚠️ 重要注意事项

### 1. 必须完整重编译
每次生成 proto 文件后，**必须关闭编辑器 → IDE 完整重新编译 → 重启编辑器**。

❌ **不可以**：
- 只用 Live Coding（可能不生效）
- 只重启编辑器不重编译（描述符不会注册）

✅ **正确做法**：
- Clean + Build（完整重编译）

### 2. 不支持的类型
以下类型**无法序列化**：
- UObject 指针（如 `AActor*`、`UTexture2D*`）
- UInterface
- 委托（Delegate）
- TWeakObjectPtr、TSoftObjectPtr

**解决方法**：只序列化基础数据（ID、名称、位置等），不序列化对象引用。

### 3. 跨语言兼容性
如果服务器用 Go/Java/Python 等其他语言：
1. 把生成的 `.proto` 文件复制给服务器
2. 服务器用对应语言的 protoc 编译器生成代码
3. 确保字段名、类型完全一致

---

## 🛠️ 故障排查

### 问题 1：运行时报错 "Proto Descriptor for XXX not found"

**原因**：生成的 `.pb.cpp` 文件未被编译。

**解决**：
1. 确认 `ProtoSource/` 目录下有 `.pb.cpp` 文件（不是 `.pb.cc`）
2. 确认执行了完整重编译（不是 Live Coding）
3. 查看编译输出，搜索 "User.pb.cpp"，确保被编译了
4. 如果还不行，删除 `Intermediate`、`Binaries` 文件夹后重新编译

### 问题 2：生成失败，提示找不到 protoc.exe

**原因**：protoc 编译器缺失。

**解决**：
1. 检查 `Plugins/UE-LinkProtobuf/Source/ThirdParty/Win64/protoc.exe` 是否存在
2. 如果不存在，从 [protobuf releases](https://github.com/protocolbuffers/protobuf/releases) 下载对应版本
3. 或者从原插件仓库重新下载

### 问题 3：Android 打包失败

**原因**：可能缺少 Android 平台的 protobuf 静态库。

**解决**：
1. 确认 `ThirdParty/Android/` 下有 `armeabi-v7a`、`arm64-v8a`、`x86_64` 三个目录
2. 每个目录下应该有 `lib/libprotobuf.a`
3. 如果缺失，从原插件仓库重新复制

### 问题 4：序列化后数据大小为 0

**原因**：结构体未在设置中添加，或编译时未生成描述符。

**解决**：
1. 检查 `Project Settings → LinkProtobuf` 中是否添加了该结构体
2. 重新生成并编译
3. 查看日志是否有 "Proto Converting struct: XXX" 消息

---

## 📚 支持的类型列表

| UE 类型 | Protobuf 类型 | 说明 |
|---------|--------------|------|
| `bool` | `bool` | ✅ 支持 |
| `int32` | `int32` | ✅ 支持 |
| `int64` | `int64` | ✅ 支持 |
| `uint32` | `uint32` | ✅ 支持 |
| `uint64` | `uint64` | ✅ 支持 |
| `float` | `float` | ✅ 支持 |
| `double` | `double` | ✅ 支持 |
| `FString` | `string` | ✅ 支持 |
| `FName` | `string` | ✅ 支持（自动转换） |
| `FText` | `string` | ✅ 支持（自动转换） |
| `uint8` (byte) | `bytes` | ✅ 支持 |
| `UENUM` | `enum` | ✅ 支持 |
| `USTRUCT` | `message` | ✅ 支持（嵌套结构体） |
| `TArray<T>` | `repeated T` | ✅ 支持 |
| `TSet<T>` | `repeated T` | ✅ 支持（重复值会去重） |
| `TMap<K,V>` | `map<K,V>` | ✅ 支持（Key 限制：int/uint/string） |

---

## 🌐 跨平台支持

| 平台 | 编辑器 | 打包 | 说明 |
|------|--------|------|------|
| Windows x64 | ✅ | ✅ | 已测试 |
| Windows ARM64 | - | ✅ | 理论支持 |
| Android (arm64-v8a) | - | ✅ | 已测试 |
| Android (armeabi-v7a) | - | ✅ | 已测试 |
| Android (x86_64) | - | ✅ | 模拟器支持 |
| Linux x64 | ⏳ | ✅ | 计划中 |
| Linux ARM64 | - | ✅ | 理论支持 |
| iOS | - | ⏳ | 计划中 |
| macOS | ⏳ | ⏳ | 计划中 |

---

## 📖 参考资料

- **原插件仓库**: https://github.com/DarkestLink-Dev/UE-LinkProtobuf
- **Protobuf 官方文档**: https://protobuf.dev/
- **UE C++ 编程指南**: https://docs.unrealengine.com/5.3/en-US/unreal-engine-cpp-programming/

---

## 📞 技术支持

如遇到问题：
1. 查看本文档的"故障排查"部分
2. 查看 UE 编辑器的 Output Log
3. 查看 IDE 的编译输出
4. 参考原插件仓库的 Issues

---

**祝你使用愉快！🎉**

---

请将以上内容**完整替换**你现有的 `修改说明.md` 文件内容。这个新版本：

✅ 更加注重实用性和操作流程  
✅ 提供了详细的代码示例  
✅ 涵盖了新项目迁移的完整步骤  
✅ 增加了常见问题排查  
✅ 结构更清晰，易于查阅

你可以直接复制上面的内容，或者切换到 agent mode 让我帮你自动替换文件。